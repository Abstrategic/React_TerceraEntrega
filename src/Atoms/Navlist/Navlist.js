import './Navlist.css'

const Navlist = (props) => {
    return (
        <li className="navlist">
            <p className="Botom" type="buttom">{props.name}</p>
        </li>
    )
    }
    export default Navlist;